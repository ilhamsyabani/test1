export default function Hero() {
    return (
        <section id="hero" className="flex items-center h-[100px] md:h-[400px] w-full bg-hero-image bg-cover bg-no-repeat">
            <div>
            </div>
        </section>
    );
}
